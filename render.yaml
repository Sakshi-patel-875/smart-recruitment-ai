services:
  - type: web
    name: smart-recruitment-ai
    env: python
    plan: free

    runtime: python3.10   # ⭐ THIS is the missing line

    buildCommand: |
      python -m pip install --upgrade pip
      pip install -r requirements.txt

    startCommand: gunicorn app:app